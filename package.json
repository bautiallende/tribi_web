{
  "name": "tribi",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "make dev",
    "lint": "echo \"add linters per app\" && exit 0",
    "build": "echo \"build per app\" && exit 0"
  }
}
